# VisionAid++ Flutter

## Setup (once)

```powershell
copy .env.example .env
```

Edit `.env` with Supabase anon key + URL.

## Run / release

```powershell
flutter run
flutter build apk --release
```

No dart-defines needed.
